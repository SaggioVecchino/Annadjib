<UserControl x:Class="Projet.View.UsrCtrl.Exercices.Exercice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projet.View.UsrCtrl.Exercices"
             mc:Ignorable="d"
             d:DesignHeight="570" d:DesignWidth="690">
    
    

    <Grid>
        <Border x:Name="backgrnd"
                Background="#FFFFFF"
                CornerRadius="3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5"
                                  ShadowDepth="0"
                                  Opacity="0.2" />
            </Border.Effect>
        </Border>

        <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="27*"/>
                    <RowDefinition Height="116*"/>
                    <RowDefinition Height="47*"/>
                </Grid.RowDefinitions>
                <ContentControl x:Name="Temps" Content="ContentControl" Margin="0,0,0,325" Grid.RowSpan="2"/>
                <ContentControl x:Name="Question" Content="ContentControl" Grid.Row="1" Margin="0,0,0,1"/>
                <ContentControl x:Name="Reponse" Content="ContentControl" Grid.Row="2" />


            </Grid>
        
    </Grid>

</UserControl>
